import 'package:flutter/material.dart';

class SelectedBuilder extends StatelessWidget {
  const SelectedBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
